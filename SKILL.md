# SKILL

## JavaScript / Node.js Skills
- Dynamic DOM extraction from platform-specific selectors.
- Headless stealth browser operation with `puppeteer-extra-plugin-stealth`.
- Automated lazy-load triggering via controlled infinite scrolling depth.
- Proxy rotation and randomized user-agent injection for scraper resilience.

## Python Skills
- IDX market ingestion using `yfinance` with `.JK` ticker enforcement.
- Technical feature engineering with `pandas` + `ta` (RSI-14, MACD, volume pct change, daily returns).
- Time-series safe training using rolling splits (`shuffle=False`) to avoid leakage.
- Indonesian social text tokenization and IndoBERT-based sentiment scoring with slang-weight adaptation.

## C++ / Java Skills
- High-throughput JSON stream parsing for ingesting model and social outputs.
- Low-latency array-based order-book and anomaly computations for short-term strategies.
- Deterministic execution risk controls (stop-loss and trigger generation).
